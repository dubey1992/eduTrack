"""The HTTP layer: request in, response out, and as little else as possible.

Split by module the way app/Http/Controllers/Api/V1 is, so a controller and
its port sit at the same path under a different extension.
"""
